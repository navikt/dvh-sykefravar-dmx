{{ config(
    materialized='table',
    tags=['GEN']
) }}

with source_arbeidsperiode_sm as (
    select  * from {{ source('dmx_pov_sykefravar_andre', 'ARBEIDSPERIODE') }}
),

final as (
    select fk_person1,PERIODE,STILLING_ANDEL,FK_KOMMUNE_ARBSTED, KOMMUNE_NUMMER_ARBSTED  from source_arbeidsperiode_sm
    where rownum < 2
)

select * from final
