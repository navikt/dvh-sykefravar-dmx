version:  2

models:
  - name: stg_dim_tid
    description: 'Tidsdimensjonen'
    columns:
      - name: pk_dim_tid
        tests:
        - unique
        - not_null

  - name: stg_dim_person1
    description: 'Persondimenjsons'
    columns:
      - name: pk_dim_person
        tests:
        - unique
        - not_null

  - name: stg_dim_organisasjon
    description: Oversikt over organisasjonen
    columns:
      - name: ek_org_node
        tests:
        - unique
        - not_null

  - name: mk_syfo_oppfolging_pvt
    description: sykefravær_info pivotert
    columns:
      - name: DIALOGMOTE_UUID
        description: unique identifikator
        tests:
        - unique
        - not_null

  - name: mk_syfo_dialogmote_join_dim_person
    description: koblet med dim person
    columns:
      - name: DIALOGMOTE_UUID
        description: unique identifikator
        tests:
        - unique
        - not_null

  - name: mk_syfo_dialogmote_join_dim_organisasjon
    description: koblet med dim organisasjon
    columns:
      - name: DIALOGMOTE_UUID
        description: unique identifikator
        tests:
        - unique
        - not_null

  - name: mk_syfo_dialogmote_join_dim_varighet
    description: Target tabell
    config:
      materialized: table
    description: Target tabell
    columns:
      - name: DIALOGMOTE_UUID
        description: unique identifikator
        tests:
        - unique
        - not_null
