version: 2

seeds:
  - name: modia_kandidat
    config:
      column_types:
        personIdentNumber: varchar2(255)
  - name: modia_dialogmote
    config:
      column_types:
        enhet_nr: varchar2(255)
